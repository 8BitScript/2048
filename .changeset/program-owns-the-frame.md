---
"2048": patch
---

The program owns its start-up and its frame. `2048.8bs` is that loop; `session.8bs` is gone. A turn is still `play.8bs`, and what is on the screen is unchanged. Release sizes are unchanged (the C64, the PET 4032, and the VIC-20 checksummed identical).
