---
"2048": patch
---

The program entry starts the session and runs a frame. `session.8bs` is start-up and that frame, `play.8bs` is a turn, and `<Game />` returns the board or the title directly. Release sizes are unchanged.
